# Student Grade Analyzer & CSV Data Cleaner

## Project 1 - Student Grade Analyzer
A Python script that processes a list of students, calculates their stats, and prints a formatted report.

## Project 2 - CSV Data Cleaner
A Python script that reads a messy CSV file, validates each row, fills in missing values, and writes the cleaned data to a new CSV file.